{
  "name": "@kaudal/curriculum-parser",
  "version": "4.0.1",
  "license": "MIT",
  "bin": {
    "curriculum-parser": "index.js"
  },
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "pretest": "eslint .",
    "test": "jest test/*.spec.js --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "changelog": "git log $(git describe --tags --abbrev=0)..HEAD --oneline --format=\"* %h %s\""
  },
  "dependencies": {
    "@kaudal/models": "^2.0.1",
    "chalk": "^5.0.1",
    "js-yaml": "^4.1.0",
    "jsdom": "^16.6.0",
    "marked": "^4.0.17",
    "minimist": "^1.2.6",
    "mongoose": "^5.13.14"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "eslint": "^8.19.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "jest": "^27.5.1"
  }
}
